import { StubPage } from '@/components/nurse-hierarchy/stub-page';

export default function PoliciesPage() {
  return (
    <StubPage
      title="Policies"
      description="Nursing policies and SOPs. Initial release will use the existing system forms framework; a dedicated policy CMS is planned as follow-up."
      phaseHint="Policies stub arrives with Phase 8."
    />
  );
}
